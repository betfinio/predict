sources:
  - name: chainlink
    handler:
      graphql:
        endpoint: ${PUBLIC_CHAINLINK_GRAPH_URL}
  - name: predict
    handler:
      graphql:
        endpoint: ${PUBLIC_PREDICT_GRAPH_URL}

codegen:
  scalars:
    BigInt: bigint
    Bytes: string

documents:
  - ./src/schema.graphql